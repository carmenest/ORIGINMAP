<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>ORIGINMAP Report</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    h1, h2, h3 { margin-top: 30px; }
    table { border-collapse: collapse; width: 100%; margin-top: 10px; }
    th, td { border: 1px solid #ccc; padding: 6px; font-size: 12px; }
    th { background: #f0f0f0; }
    img { max-width: 100%; margin: 20px 0; }
    .meta { font-size: 12px; color: #555; }
  </style>
</head>
<body>

<h1>{{ project }}</h1>
<p class="meta">
Generated (UTC): {{ generated_utc }}<br>
Run ID: {{ manifest.run_id }}<br>
Python: {{ manifest.python_version }}<br>
Platform: {{ manifest.platform }}
</p>

<h2>Datasets</h2>
<pre>{{ manifest.inputs | tojson(indent=2) }}</pre>

<h2>Metrics Summary</h2>
<pre>{{ metrics | tojson(indent=2) }}</pre>

{% if diversity %}
<h2>Diversity by Class</h2>
<table>
  <tr>
    {% for k in diversity[0].keys() %}
    <th>{{ k }}</th>
    {% endfor %}
  </tr>
  {% for row in diversity %}
  <tr>
    {% for v in row.values() %}
    <td>{{ v }}</td>
    {% endfor %}
  </tr>
  {% endfor %}
</table>
{% endif %}

{% if figures %}
<h2>Figures</h2>
{% for fig in figures %}
  <h3>{{ fig }}</h3>
  <img src="{{ fig }}" alt="{{ fig }}">
{% endfor %}
{% endif %}

<h2>Provenance</h2>
<p>Manifest file: {{ manifest.run_id }}</p>

</body>
</html>
